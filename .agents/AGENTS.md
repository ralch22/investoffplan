# Deployment Rule

When completing a requested feature, bug fix, or interaction design task:
1. Ensure the code successfully compiles and passes tests.
2. Automatically run `npm run deploy:production` in the background.
3. You do NOT need to wait for the user to explicitly say "Deploy to main" or "Deploy to CF". Just launch the deployment automatically as part of the completion of your workflow.
